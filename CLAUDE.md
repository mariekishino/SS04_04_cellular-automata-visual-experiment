# CLAUDE.md — コーディングエージェントへの常時ルール

**読者:** コーディングエージェント(Claude Code など)。プロジェクトを開くたびに自動で読み込まれる。
**役割:** どのフェーズでも変わらないルールを置く。人間向けの説明は [docs/](docs/) にある。

## 最初に読むもの

1. [README.md](README.md) の「現在のフェーズ」。ここに書かれたフェーズだけが作業範囲。
2. [docs/phases/](docs/phases/) の該当フェーズ文書。実装範囲と完了条件はここが正本。
3. [docs/01_architecture.md](docs/01_architecture.md) と [docs/02_experiment_protocol.md](docs/02_experiment_protocol.md)。

## やること

- 現在のフェーズに必要な最小のコードと観察手段を実装し、ビルドして実行する。
- 設定、実際に見えた挙動、数値、うまくいかなかった条件を記録する。
- C++ のデータ構造、メモリ所有、更新順序、計算量を説明する。本人の学習が目的の一つ。
- 設計を変えるときは [docs/decisions/](docs/decisions/) に、問い・決定・理由・見送った案・再検討条件を短く残す。
- フェーズが終わったら `/phase-report` の様式で報告し、本人の確認を待つ。

## やらないこと

- 先のフェーズを先取りしない。音声入力、履歴、可塑性、LLM、DJ Node Tool 連携、CUDA/GPU、FFT、複数個体は、該当フェーズになるまで実装しない。
- 観察していないことを書かない。会話中の候補を「実験した」ことにして記録に入れない。
- 画面が派手なことを数値の健全性の代わりにしない。NaN/Inf、消滅、全面飽和は演出ではなく失敗として記録する。
- 「適応」「記憶」「生命」「進化」を確認済みの事実として書かない。定義した観測値で述べる。
- 本人の確認なしに次のフェーズへ進まない。

## 技術の前提

- C++17、CMake。42 課題の C++98 縛りは持ち込まない。
- シミュレーション中核に描画・音声ライブラリの型を持ち込まない。
- 2 枚の盤面で同期更新する。更新中に書き込み先の盤面を読まない。
- 固定時間刻み。描画フレーム数から切り離す。
- CPU の小さい格子から始め、性能は測ってから最適化する。

## Git の運用

- **ブランチはフェーズごとに 1 本。** 名前は `phase-0-model-exploration` のように、番号と内容を入れる。フェーズ開始時に本人が作る。なければ作ってよい。
- **commit はローカルで、意味のある区切りごとに行う。** 例: 1 つのモデルが動いた、指標を追加した、境界処理を直した。1 commit に複数モデルの変更を混ぜない。
- **push と PR 作成は行わない。** フェーズ完了後に本人が行う。`gh` コマンドも使わない。
- **実験記録の `code_version`** には、その実験を行った時点のコミットハッシュを書く。未コミットの状態で実験した数値は、記録に「未コミット」と明記する。

## テスト

境界処理、同じ設定の再実行、数値が有限であること。実際の失敗を検出する項目に絞る。
